document.addEventListener("DOMContentLoaded", function () {
    let video = document.getElementById("bg-video");
    video.playbackRate = 0.5; // Ajuste a velocidade: 1 = normal, 0.5 = mais lento, 2 = mais rápido
});
